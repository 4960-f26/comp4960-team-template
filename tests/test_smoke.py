"""Smoke test so CI is green from day one.

Replace and extend this with real tests as you build. A meaningful test
checks your own logic and would FAIL if that logic were wrong — see Lab 8
(CI as the gate) and the L13/L14 testing lectures.
"""


def test_smoke():
    assert True
